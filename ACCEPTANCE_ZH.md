# Illustrator Windows 加固版验收记录

发布日期：2026-09-17。基线：Windows、Illustrator 2024 (28.6.0)、Python 3.12.14。
本记录只总结实际证据；真实素材、原始日志、截图及个人路径不随项目发布。

## 实测通过

- 本次发布前自动化回归：32 项 unittest 全部通过，包含 Node 执行的状态脚本检查。
- 历史实机测试：合成素材和约 162 MB AI 文件副本的编辑、保存、重开；多画板、剪切组、可编辑中文及缺失链接状态。
- 独立连续运行：3604 秒、650 次循环、10 次重连、12 次预览成功；43 次最小化截图拒绝单独计数，未当作截图成功。结束后保存重开核对结构和最终文字。
- 当前 Codex 对话真实工具入口：三轮“明确目标 → 编辑 → 截图核对 → 保存 → 关闭 → 重开”。核对矩形颜色、可编辑中文及对照文档未变化。
- 当前 Codex 入口各三轮：不存在目标、多文档未指定目标、目标关闭后调用；均拒绝写入。
- 执行中超时三轮：返回结果未知，阻止后续写入；读取实际状态、显式恢复后才继续。超时前已开始的写入可能完成，不承诺自动撤销。

## 支持范围及未验收项

- 9 个 MCP 工具中，4 个为控制/状态工具：`run`、`get_state`、`view`、`recover_connection`；其余 5 个为提示和帮助。
- 当前 Codex 原生入口的重载/断线重连和真实排队过期仍未完成验收；不能用独立 stdio 测试替代。原生并发实验实际触发了执行中超时，未登记为排队过期通过。
- macOS、其他 Illustrator 版本、任意多窗口及其他 DPI/显示器组合尚未实机认证。
- emoji 字符串可往返，但视觉显示仍取决于字体。ExtendScript 不能假定存在 `JSON.stringify`。
- `run` 执行可信 JSX，不是沙箱；脚本能够主动切换文档、访问文件和覆盖输出。调用者必须审查脚本和保存路径。
- 互斥只覆盖使用相同安全目录的本加固版客户端，不覆盖人工操作或其他脚本。

## 升级后快速回归

1. 使用 Python 3.12，在新目录执行安装脚本，保留旧环境以便切回。
2. Node.js 放入 PATH，运行 `.venv\Scripts\python.exe -m unittest discover -s tests`，确认无跳过。
3. MCP 客户端重载后确认 9 个工具可发现，读取应用版本和文档状态。
4. 创建两个专用测试文档，用明确的 `target_path` 修改其中一个，截图、保存、关闭、重开，核对内容和对照文档；连续三轮。
5. 对不存在目标、关闭目标、多文档省略目标分别检查拒绝结果。仅在专用测试文件中测试超时；遇到 `outcome_unknown` 先读状态，核对部分修改，再调用 `recover_connection(acknowledge=true)`，不得直接重试写入。

自动化通过不代表实机通过；发生部分修改时恢复连接不会回滚文档。
